# Copy to .env.local and fill in.

# Postgres connection string. Matches the docker-compose `db` service by default.
DATABASE_URL=postgresql://postgres:mysecretpassword@localhost:5432/archive

# Random secret used to sign sessions — generate with `openssl rand -base64 32`
# (or `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`).
BETTER_AUTH_SECRET=

# Where the app is served from. Same value for both in local dev.
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
